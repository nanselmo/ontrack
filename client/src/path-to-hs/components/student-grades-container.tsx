import * as React from "react";

import StudentGrades from "shared/types/student-grades";

interface StudentGradesContProps {
  defaultGrades: StudentGrades | null
  onChange: (newGrades: StudentGrades) => any
}

const StudentGradesContainer = (props: StudentGradesContProps) => {
  return null;
}

export default StudentGradesContainer;
