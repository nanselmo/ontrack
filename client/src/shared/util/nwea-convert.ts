import ScoreType from "shared/enums/score-type";

export const percentileToRit = (percentile: number, testType: ScoreType, gradeLevel: number): number => {
  // TODO: Implement me!
  return 0;
};

export const ritToPercentile = (rit: number, testType: ScoreType, gradeLevel: number): number => {
  // TODO: Implement me!
  return 0;
};
