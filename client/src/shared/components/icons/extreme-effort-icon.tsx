import * as React from "react";

import IconComponentProps from "shared/types/icon-component-props";
import IconComponent from "shared/types/icon";
const ExtremeEffortIcon: IconComponent = (props) => {
  return (
    <div style={props.style}>
      

    </div>
  ) 

};

export default ExtremeEffortIcon;
