[WIP] Redesign HsCalc component: redesign StudentScoreDisplay

* rename HSCalc -> StudentInfoDisplay
* restructure StudentInfoDisplay (previously HSCalc)
  ** new child component ReportCard 
  ** refactor ScoreField
* rewrite score transformation code
  ** replace ScoreTransformerSettings component and 
    CreateScoreTransformer higher-order-function with local functions
    in StudentInfoDisplay
* move ScoreType and EffortLevel enums from shared/types -> shared/enums      
* add type StudentScore to shared/types
* edit interface StudentScores so that all properties of type StudentScore
* rename shared/util/grade-conversion -> shared/util/gradeconvert
* add new methods to shared/util/grade-conveart
  ** stringToScore: string, ScoreType
  ** scoreToString: StudentScore, ScoreType
  ** scoreToPercentile: StudentScore, number in [1,100]
  ** percentileToScore: number, StudentScore

* WIP scaffold new util NWEAConvert
  ** RitToPercentile(rit: number, ScoreType, gradelevel: number)
  ** PercentileToRit(percentile: number, ScoreType, gradelevel: number)
