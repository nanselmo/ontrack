Implement config system

* add config/ folder to top level
  ** add data/ folder to contain raw data in json form
    *** move tract-tier-table and rit-percentile-lookup
      data from shared/data to config/data
    *** move hs requirement function definitions from
      hs-config-data to config/requirement_function_definitions.js
    *** 

  ** TODO write build code to compile a version of hs_programs
    with requirement function names baked in, to avoid making
    a trillion function calls to hs_requirement_function every
    time state changes

