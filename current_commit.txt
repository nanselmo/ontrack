Refactor HSProgramsContainer to use immutableJS

* DONE rewrite HSProgramsContainer mapStateToProps using
  immutablejs and memoize using reselect
* DONE rewrite HSProgramsContainer mapDispatchToProps using
  immutablejs and memoize using reselect
* DONE change appState type to make state.hsData.hsProgramIDsByType
  into a plain old javascript object, not an immutableJS object.
  (We don't expect it to change, so immutableJS offers no benefit.)
